.card.border-secondary.mb-3
  .card-header
    = name
  .card-body
    %p.card-text
      quantity: 
      = amount
      &nbsp;
      price:
      = price
  .card-footer
    %p.card-text
      total price:
      = price*amount
      - @total_price += price*amount
      - puts @total_price